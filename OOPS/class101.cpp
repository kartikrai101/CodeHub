#include<bits/stdc++.h>
using namespace std;

class nba{  // class named nba
    public:

    // string name;
    // int mvps;
    string team;
    float netWorth;
};

int main(){
    nba a;  // instance of the class nba (an object of type 'nba')

    // a.name = "Michael";
    // a.team = "Chicago";
    // a.mvps = 6;
    // a.netWorth = 1.7;

    cout << sizeof(a) <<endl;
    // cout << sizeof(a.name) << " " << sizeof(a.team) << " "<< sizeof(a.mvps) << " "<< sizeof(a.netWorth);
}